AgentMeter {{VERSION}} for macOS (build {{BUILD}}).

## What's new

- Added Cursor usage tracking alongside the existing coding-plan providers.
- Added support for Cursor team usage when a team administrator configures the required credentials.
- Expanded the Mac provider settings and menu-bar views with Cursor status, plan details, and usage windows.
- Added an App Store download section in Mac settings for installing the companion iPhone and Apple Watch app.
- Updated all supported localizations for the new Cursor and App Store settings.

## Security and privacy

- Cursor authentication is detected from the existing local sign-in in read-only mode.
- Optional Cursor team credentials remain in the local Keychain and are never included in CloudKit records or diagnostics.
- CloudKit receives only sanitized quota snapshots, never provider credentials.

## Download verification

- Universal macOS build for Apple Silicon and Intel.
- Developer ID signed, Apple-notarized, stapled, and Gatekeeper verified.
- SHA-256: `{{SHA256}}`

---

## 中文说明

AgentMeter {{VERSION}} macOS 版（构建号 {{BUILD}}）。

### 新增内容

- 新增 Cursor 用量追踪，与现有编程套餐服务商统一展示。
- 支持 Cursor 团队管理员配置所需凭据后查看团队用量。
- 扩展 Mac 服务商设置与菜单栏视图，展示 Cursor 状态、套餐详情和用量窗口。
- 在 Mac 设置中新增 App Store 下载区域，便于安装配套的 iPhone 与 Apple Watch app。
- 为 Cursor 与 App Store 新设置补齐所有已支持语言的本地化。

### 安全与隐私

- Cursor 登录状态仅从本机已有登录信息中以只读方式检测。
- 可选的 Cursor 团队凭据只保存在本机 Keychain，不会进入 CloudKit 记录或诊断报告。
- CloudKit 只接收清洗后的额度快照，绝不包含服务商凭据。

### 下载验证

- 通用 macOS 构建，同时支持 Apple 芯片和 Intel Mac。
- 已使用 Developer ID 签名、通过 Apple 公证并完成装订，且通过 Gatekeeper 验证。
- SHA-256：`{{SHA256}}`
