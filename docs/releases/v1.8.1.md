AgentMeter {{VERSION}} for macOS (build {{BUILD}}).

## What's fixed

- Restored the signed application and team identifiers required to initialize the Production CloudKit container.
- Fixed Mac-to-iPhone quota synchronization after reinstalling the iPhone app.
- Added release checks that reject a DMG when its signature, notarization, application identifier, team identifier, CloudKit service, container, or Production environment is missing or invalid.

## Upgrade notes

- Replace AgentMeter 1.8 with this build, then refresh once on the Mac to publish a new quota snapshot to CloudKit.
- Open the iPhone app again after the Mac reports a successful merged write.

## Download verification

- Universal macOS build for Apple Silicon and Intel.
- Developer ID signed, Apple-notarized, stapled, and Gatekeeper verified.
- SHA-256: `{{SHA256}}`

---

## 中文说明

AgentMeter {{VERSION}} macOS 版（构建号 {{BUILD}}）。

### 修复内容

- 恢复初始化 Production CloudKit 容器所需的已签名 Application Identifier 和 Team Identifier。
- 修复 iPhone App 重装后，Mac 额度无法重新同步到 iPhone 的问题。
- 新增发布门禁：DMG 的签名、公证、Application Identifier、Team Identifier、CloudKit 服务、容器或 Production 环境任一缺失或无效时，发布构建会直接失败。

### 升级说明

- 请用此版本替换 AgentMeter 1.8，然后在 Mac 上刷新一次，将新的额度快照写入 CloudKit。
- Mac 日志确认“已合并写入”后，再打开 iPhone App 刷新。

### 下载验证

- 通用 macOS 构建，同时支持 Apple 芯片和 Intel Mac。
- 已使用 Developer ID 签名、通过 Apple 公证并完成装订，且通过 Gatekeeper 验证。
- SHA-256：`{{SHA256}}`
