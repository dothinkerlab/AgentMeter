AgentMeter {{VERSION}} for macOS (build {{BUILD}}).

## What's new

- Added full Light and Dark appearance support to the menu-bar panel.
- Replaced fixed interface colors with adaptive macOS semantic colors for text, separators, controls, loading states, warnings, and quota status.
- Added adaptive provider brand and status colors with verified contrast in both appearances.

## Security and privacy

- This release does not change credential storage, network access, CloudKit data, or diagnostics behavior.
- Provider credentials remain local to the device and are never included in CloudKit records or diagnostics.

## Download verification

- Universal macOS build for Apple Silicon and Intel.
- Developer ID signed, Apple-notarized, stapled, and Gatekeeper verified.
- SHA-256: `{{SHA256}}`

---

## 中文说明

AgentMeter {{VERSION}} macOS 版（构建号 {{BUILD}}）。

### 新增内容

- 菜单栏面板现已完整支持浅色与深色外观。
- 将文字、分隔线、控件、加载状态、告警和额度状态的固定颜色替换为自适应 macOS 语义色。
- 为服务商品牌色和状态色加入明暗模式变体，并验证两种外观下的对比度。

### 安全与隐私

- 本次更新不改变凭据存储、网络访问、CloudKit 数据或诊断行为。
- 服务商凭据仍只保存在本机，不会进入 CloudKit 记录或诊断报告。

### 下载验证

- 通用 macOS 构建，同时支持 Apple 芯片和 Intel Mac。
- 已使用 Developer ID 签名、通过 Apple 公证并完成装订，且通过 Gatekeeper 验证。
- SHA-256：`{{SHA256}}`
