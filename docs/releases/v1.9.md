AgentMeter {{VERSION}} for macOS (build {{BUILD}}).

## What's new

- Added health alerts for quota collection failures, CloudKit sync failures, and unavailable Codex reset-credit data.
- Show red alerts for visible providers and yellow warnings for hidden providers, with issue details in the menu-bar panel.
- Consolidated the About version display to include the version, build number, build configuration, and CloudKit environment.
- Added automated macOS tests for health alerts and build metadata, plus release checks that verify CloudKit environment metadata matches the signed app entitlement.

## Download verification

- Universal macOS build for Apple Silicon and Intel.
- Developer ID signed, Apple-notarized, stapled, and Gatekeeper verified.
- SHA-256: `{{SHA256}}`

---

## 中文说明

AgentMeter {{VERSION}} macOS 版（构建号 {{BUILD}}）。

### 新增与改进

- 新增健康告警，提示额度采集失败、CloudKit 同步失败及 Codex 重置额度数据不可用等问题。
- 已显示的服务商异常使用红色告警，已隐藏的服务商异常使用黄色警告，并在菜单栏面板中提供问题详情。
- 统一“关于”中的版本信息，显示版本号、构建号、构建配置及 CloudKit 环境。
- 新增健康告警与构建信息的 macOS 自动化测试，并在发布校验中确认 CloudKit 环境信息与应用签名权限一致。

### 下载验证

- 通用 macOS 构建，同时支持 Apple 芯片和 Intel Mac。
- 已使用 Developer ID 签名、通过 Apple 公证并完成装订，且通过 Gatekeeper 验证。
- SHA-256：`{{SHA256}}`
