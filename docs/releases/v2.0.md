AgentMeter {{VERSION}} for macOS (build {{BUILD}}).

## What's new

- Added Codex automatic resume monitoring for sessions interrupted by quota limits, with resume eligibility checks and Desktop queue submission.
- Added a persistent resume queue and history, duplicate-submission protection, session diagnostics, and manual resume controls.
- Added in-app update checks with links to the latest GitHub release and download.
- Simplified resume status messages and reduced attention notifications.

## Download verification

- Universal macOS build for Apple Silicon and Intel.
- Developer ID signed, Apple-notarized, stapled, and Gatekeeper verified.
- SHA-256: `{{SHA256}}`

---

## 中文说明

AgentMeter {{VERSION}} macOS 版（构建号 {{BUILD}}）。

### 新增与改进

- 新增 Codex 自动续跑监控：识别因额度限制中断的会话，检查续跑条件并提交到 Desktop 队列。
- 新增持久化续跑队列与历史记录、重复提交保护、会话诊断及手动续跑操作。
- 新增应用内更新检查，可前往最新 GitHub Release 查看说明并下载更新。
- 精简续跑状态文案，减少需要关注类通知的打扰。

### 下载验证

- 通用 macOS 构建，同时支持 Apple 芯片和 Intel Mac。
- 已使用 Developer ID 签名、通过 Apple 公证并完成装订，且通过 Gatekeeper 验证。
- SHA-256：`{{SHA256}}`
